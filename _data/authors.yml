# Authors map. Used by _includes/author-profile.html when a page sets
# `author: "Name"` in front-matter. Site-wide author info comes from
# _config.yml `author:` block instead, so this map stays empty.
{}
